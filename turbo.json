{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "env": ["SIMPLE_ICONS_VERSION", "NODE_OPTIONS"]
    },
    "publish": {
      "env": ["BUN_AUTH_TOKEN"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}